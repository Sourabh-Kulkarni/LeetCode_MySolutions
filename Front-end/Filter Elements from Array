/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var filter = function(arr, fn) {
    let finalAns = [];
    for(let i=0;i<arr.length;i++){
        if(fn(arr[i],i)){
            finalAns.push(arr[i])
        }
    }
    return finalAns;
};
